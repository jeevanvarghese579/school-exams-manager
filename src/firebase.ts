import { initializeApp } from 'firebase/app'; import { getAuth, GoogleAuthProvider, signInWithPopup, signOut } from 'firebase/auth'; import { doc, getFirestore, setDoc } from 'firebase/firestore'; import type { Project } from './models';
const config={apiKey:import.meta.env.VITE_FIREBASE_API_KEY,authDomain:import.meta.env.VITE_FIREBASE_AUTH_DOMAIN,projectId:import.meta.env.VITE_FIREBASE_PROJECT_ID,storageBucket:import.meta.env.VITE_FIREBASE_STORAGE_BUCKET,messagingSenderId:import.meta.env.VITE_FIREBASE_MESSAGING_SENDER_ID,appId:import.meta.env.VITE_FIREBASE_APP_ID};
export const firebaseReady=Boolean(config.apiKey&&config.projectId); const app=firebaseReady?initializeApp(config):undefined; export const auth=app?getAuth(app):undefined; const store=app?getFirestore(app):undefined;
export const googleSignIn=()=>auth?signInWithPopup(auth,new GoogleAuthProvider()):Promise.reject(new Error('Firebase is not configured. Add environment variables first.')); export const logOut=()=>auth?signOut(auth):Promise.resolve(); export const syncProject=async(uid:string,p:Project)=>{if(!store)throw new Error('Firebase unavailable');await setDoc(doc(store,'users',uid,'projects',p.id),p);};
